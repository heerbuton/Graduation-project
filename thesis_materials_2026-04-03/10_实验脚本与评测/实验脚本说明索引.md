# 实验脚本说明索引

## 1. 后端流程与功能验证

1. `run_backend_pipeline_test.py`：后端流水线连通性验证
2. `test_app_score_model.py`：后端输出与渲染模型相关测试
3. `test_llm_module.py`：LLM 模块行为验证
4. `test_score_model_transformer.py`：LLM 到 ScoreModel 转换验证
5. `test_score_model_script.py`：打谱 JSON 转换脚本测试

## 2. 聚类与拓扑相关

1. `test_clustering.py`：基础聚类验证
2. `test_clustering_fullpage.py`：全页样本聚类验证
3. `test_clustering_sahi.py`：SAHI 场景下聚类验证
4. `test_group_order_on_testpicture.py`：`group_x` 顺序验证
5. `clustering_common.py`：聚类公共逻辑

## 3. 训练/推理与评测相关

1. `sahi_v07_inference.py`：SAHI 推理脚本
2. `train_v07_finetune.py`：YOLO 微调训练脚本
3. `evaluate_sahi_kfold_vote_fusion.py`：多模型投票融合评测

## 4. 工具脚本

1. `convert_llm_json_to_score_model.py`：将 LLM 输出转为前端渲染 JSON
2. `Use.md`：脚本使用说明

## 5. 论文写作建议

1. 将“训练与评测脚本”放在“实验设置”章节。
2. 将“聚类/顺序验证脚本”放在“关键方法正确性验证”章节。
3. 将“转换脚本”放在“工程实现细节”或“附录”章节。

